function normalize(text) {
	const filters = [
		/* [،؛].. -> [،؛] */
		[/([،؛])+/g, '$1'],
		
		/* [،؛.!؟:] -> [،؛.!؟:]\s */
		[/ *([،؛\.!؟:]) */g, '$1 '],

		/* !\s! -> !! */
		[/(! +){2}/g, '!!'],

		/* ?\s? -> ?? */
		[/(؟ +){2}/g, '؟؟'],

		/* :\s: -> :: */
		[/(: +){2}/g, '::'],
		
		/* .\s. -> .. */
		[/(\. +){2}/g, '..'],

		/* .\w -> . \w */
		[/\.(\w)/g, '. $1'],

		/* .... -> ... . */
		[/(\.{2,})\./g, '$1 .'],

		/* [،؛.!؟:]\w -> [،؛.!؟:]\s\w */
		[/ *([،؛\.!؟:])(\w)/g, '$1 $2'],
		
		/* (( -> « */
		[/\(( *\()+ */g, '«'],

		/* )) -> » */
		[/ *\)( *\))+/g, '»'],

		/* << -> « */
		[/<( *<)+ */g, '«'],

		/* >> -> » */
		[/ *>( *>)+/g, '»'],

		/* :« -> : « */
		[/: *«/g, ': «'],

		/* «\s -> « */
		[/« +/g, '«'],

		/* \s» -> » */
		[/ +»/g, '»'],

		/* \s.. -> \s */
		[/ {2,}/g, ' '],
	]
	
	/* Apply Filters */
	for(const filter of filters)
		text = text.replace(filter[0], filter[1])
	
	/* Trim Text */
	text = text.trim()

	return text
}
